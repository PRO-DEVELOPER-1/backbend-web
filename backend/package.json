
{
  "name": "bera-hosting-backend",
  "version": "1.0.0",
  "description": "Bera Hosting backend server",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build-frontend": "cd ../frontend && npm install && npm run build && cp -r build ../backend/public"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "dockerode": "^3.3.4",
    "express": "^4.18.2",
    "fs-extra": "^11.1.1",
    "simple-git": "^3.15.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
